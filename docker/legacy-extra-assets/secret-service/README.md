# Secret Service

CLI skeleton for managing secrets — stores and retrieves secrets for other tools. Built to run on Node >= 24.

Similar build and packaging flow to `meilisearch-cleanup` — supports `build`, `build:bundle`, and `build:standalone`.
